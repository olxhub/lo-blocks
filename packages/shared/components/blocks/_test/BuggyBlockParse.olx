<!-- Parse-time failure (native Error thrown from the block's parser). The throw
     is downgraded to a recoverable ErrorNode for this block (the file still
     parses); surfaced by collectErrors and rendered as a DisplayError. -->
<BuggyBlock id="BuggyBlockParse" throws="parse" kind="native"
            message="BuggyBlock: deliberate parse-time failure (native Error)"/>
