# config/content-sources.yaml — default content sources (COMMITTED).
#
# This is the committed default. A gitignored config/content-sources.local.yaml,
# IF PRESENT, replaces this file wholesale (not merged) — see contentSources.ts
# and docs/content-in-git.md. The default below serves everything in ./content,
# reproducing pre-multi-repo behavior, so a fresh clone runs with no local config.
#
# A source is mounted at its key (the path prefix it appears under). Forms:
#   <mount>: <dir>                                  # a checkout on disk (writable)
#   <mount>:                                        # a checkout on disk, explicit
#     dir: <dir>                                    # writability. Directories are
#     writable: false                               # writable unless set false.
#   <mount>:                                        # a git remote, in-memory
#     repo: <url>
#     branch: main            # optional, default main
#     cooldownSeconds: 60     # optional remote head-check throttle
#     tokenEnv: REPO_PAT      # optional; env var holding a PAT for private
#                             # reads and pushes. Defaults to LO_GITHUB_TOKEN
#                             # (the one-PAT case); set only to use a different
#                             # PAT for this repo. Anonymous if neither is set.
# Each source declares its own namespace (manifest.yaml, or — for repo
# sources — the repo name). This file is WHERE content lives, not WHAT it is.

sources:
  edu.memphis.psych:
    repo: https://github.com/olxhub/edu.memphis.psych.git
  edu.memphis.writing:
    repo: https://github.com/olxhub/edu.memphis.writing.git
  # edu.gsu.interdisciplinary:              # non-public repo — uncomment in
  #   repo: https://github.com/olxhub/edu.gsu.interdisciplinary.git  # content-sources.local.yaml
  #                                         # to use locally with a PAT

# Unrouted paths (demos, transitional content).
fallback: ./content

# Is the fallback (./content) editable? Default false — a deploy shouldn't let
# anyone write the bundled content. Set true in config/content-sources.local.yaml
# for local dev when you want to edit ./content from Studio / the catalog.
#
# To override any source above (e.g. make repos writable, add private repos),
# copy this file to config/content-sources.local.yaml and edit there. The local
# file replaces this one wholesale (not merged) — see contentSources.ts.
fallbackWritable: false
