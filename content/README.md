# System content

This is content installed with every lo-blocks install.

Demos and development content served straight from `./content` — the
default source on a fresh clone. Right now, this is grab-bag of
example activities across block types, used for local authoring and
testing rather than a single course, but it will evolve into something
more coherent in the future.
